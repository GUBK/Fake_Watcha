<main role="main" class="col-sm-9 ml-sm-auto col-md-10 pt-3">
  <h1>관리자 페이지</h1>
  <h2>영화 현황</h2>
  <div class="table-responsive">
    <table class="table table-striped">
      <thead>
        <tr>
          <th>#</th>
          <th>제목</th>
          <th>장르</th>
          <th>국가</th>
          <th>감독</th>
          <th>포스터</th>
          <th>삭제</th>
        </tr>
      </thead>
      <tbody>
        <% @movies.each do |movie| %>
          <tr>
            <td><%= movie.id %></td>
            <td><%= movie.title %></td>
            <td><%= movie.genre %></td>
            <td><%= movie.nation %></td>
            <td><%= movie.directors %></td>
            <td><img width="30" height="30" src="<%= movie.poster %>"></td>
            <td>
              <%= link_to "삭제", "/admin/movies_destroy/#{movie.id}", method: :delete, data: {confirm: "정말 지우시겠습니까?"} %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</main>
