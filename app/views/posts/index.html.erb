<!-- <p id="notice"><%#= notice %></p> -->

<h1>Listing Posts</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.content %></td>
        <!-- <td><%#= post.user_id %></td> -->
        <td><%= post.user.nickname %></td>
        <!-- user와 post에서 상호소속관계 설정하면 이렇게 쓸 수 있다. -->
        <td><%= link_to 'Show', post %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>
